# PUBLIC_SAFE_CHECKLIST.md

> 本文件说明工具是否含凭证类秘密、内部资料可见范围和使用边界
>
> 版本: v2.0 | 审查日期: 2026-06-10

---

## 一、凭证类秘密检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| SmartBI 账号密码 | ❌ 已排除 | `config.example.json` 不含真实凭据，需用户自行配置 |
| SmartBI Report ID | ❌ 已排除 | 使用 `<YOUR_SMARTBI_REPORT_ID>` 占位符 |
| 钉钉 Webhook Key | ❌ 已排除 | 使用 `<YOUR_DINGTALK_WEBHOOK_KEY>` 占位符 |
| 钉钉签名密钥 | ❌ 已排除 | 默认值为空字符串 |
| 飞书 App Token | ❌ 已排除 | 不在本仓库范围内（依赖 lark-cli 外部配置） |
| `.env` / `.private` 文件 | ❌ 已排除 | 已在 `.gitignore` 中排除 |
| 硬编码 Token / Cookie | ❌ 已排除 | 所有脚本通过 `config.json` 或环境变量读取 |
| 原始导出报表 (Excel/CSV) | ❌ 已排除 | 输出目录在 `.gitignore` 中 |
| 运行日志 | ❌ 已排除 | 仅在本地生成，不纳入版本控制 |

**结论：公开仓库版本不含任何真实凭证。**

---

## 二、内部资料可见范围

### 安全可公开

| 内容 | 说明 |
|------|------|
| Python 脚本源码 | 不含硬编码密码，算法逻辑可公开 |
| SKILL.md / README.md | Skill 定义和安装文档 |
| 配置模板 (`config.example.json`) | 使用占位符，不含真实值 |
| 信号分类体系 (`signal_taxonomy.md`) | 通用 NLP 语义分析方法 |
| 评分规则 (`scoring_rules.yaml`) | 评分公式和阈值，不涉及具体学员 |
| 报告模板 | Markdown/Excel 模板，无真实数据 |
| 安全策略文档 | 安全检查清单和边界定义 |
| 架构文档 (`docs/`) | 流程说明、设计文档、业务口径 |

### 需审查后公开

| 内容 | 风险点 | 审查要求 |
|------|--------|---------|
| 包含学员数量的统计摘要 | 可能泄露业务规模 | 脱敏到数量级或用百分比替代 |
| LP 姓名/ID 关联的分析结果 | 个人隐私 | 匿名化处理 |
| 话术模板 | 可能含真实学员信息 | 替换为抽象占位符 |
| 过程分析格式说明 | 可能含业务策略 | 检查是否涉及保密信息 |

### 绝对不可公开

| 内容 | 原因 |
|------|------|
| 真实 LP-家长对话数据 | 涉及学员/家长隐私、商业机密 |
| 原始 SmartBI 报表 | 包含完整业务明细 |
| 运行日志文件 | 可能含中间计算数据和时间戳 |
| 钉钉/飞书的实际推送截图 | 可能含未脱敏数据 |
| 自动化调度配置中的 Webhook Key | 生产环境凭证 |

---

## 三、使用边界

### 许可用途

- ✅ 团队内部 LP 续费意向分析
- ✅ 自动化周报/日报生成与推送
- ✅ LP 绩效评估参考
- ✅ 续费策略复盘与优化

### 禁止用途

- ❌ 将原始学员/家长对话数据用于训练外部 AI 模型
- ❌ 将分析结果出售或提供给第三方
- ❌ 在公开场合（如外部文档、社交媒体）引用未经脱敏的数据
- ❌ 用于自动决策系统（如自动升降级、自动分配 LP），需人工审核
- ❌ 绕过数据脱敏流程直接公开分析结果

### 引用规范

如需在内部文档或会议中引用本工具的分析结果，应遵循：
1. 数据脱敏：人数使用数量级（如 "约 200 人"），比例使用百分比
2. 归类引用：不可引用单个学员的具体对话内容
3. 注明来源：标注"数据来源：LP续费意向盘自动化（方案E评分）"

---

## 四、合规声明

| 维度 | 状态 | 说明 |
|------|------|------|
| 数据隐私 | ✅ 合规 | 分析仅在本地运行，不上传原始数据到第三方服务 |
| 个人信息保护 | ✅ 合规 | 公开版本不含任何学员/LP 个人信息 |
| 自动化决策 | ⚠️ 注意 | 评分结果辅助人工决策，不作为唯一依据 |
| 第三方依赖 | ⚠️ 注意 | 依赖 SmartBI (Playwright 自动化)、飞书 (lark-cli)、钉钉 (Webhook) |

---

*审查人：VIPTHINK LP 效能团队 | 下次审查：2026-12-10*
